<table class="table">
  <thead>
    <tr>
      <th scope="col"><%= items[0].category %></th>
    </tr>
  </thead>
  <tbody>
    <% for(const item of items) { %>
      <tr>
        <td><%= item.name %></td>
        </td>
      </tr>
    <% } %>
  </tbody>
</table>
